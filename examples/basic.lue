
fn fib(n: int): int {
    if n <= 1 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

fn add(x: int, y: int): int {
    x + y
}


fn main() {
    let result = fib(5)
    let result2 = add(1, 3)
    println(result)
    println(result2)
}